pipeline:
  backend-test:
    image: hseeberger/scala-sbt
    commands:
      - sbt test

services:
  database:
    image: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_DB=toctoc
