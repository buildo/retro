db {
  host = "localhost"
  host = ${?DB_SERVER}
  port = 5432
  port = ${?DB_PORT}
  user = "postgres"
  user = ${?DB_USER}
  password = ""
  password = ${?DB_PASSWORD}
  database = "toctoc"
  database = ${?DB_NAME}
}
