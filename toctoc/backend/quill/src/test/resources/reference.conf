db {
  host = "localhost"
  host = ${?DB_HOST}
  port = 5432
  port = ${?DB_PORT}
  user = "postgres"
  user = ${?DB_USER}
  password = ""
  password = ${?DB_PASSWORD}
  database = "toctoc"
  database = ${?DB_DB}
}
