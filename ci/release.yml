platform: linux

image_resource:
  type: docker-image
  source:
    repository: hseeberger/scala-sbt
    tag: 8u181_2.12.8_1.2.8

inputs:
  - name: backend

params:
  PGP_PASSPHRASE:
  PGP_SECRET:
  SONATYPE_USERNAME:
  SONATYPE_PASSWORD:

run:
  dir: backend
  path: sbt
  args:
    - ci-release
